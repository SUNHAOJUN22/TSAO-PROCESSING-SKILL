name: TSAO CI

on:
  push:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: python -m pip install --quiet -e .[dev]
      - name: Dependency integrity
        run: python -m pip check
      - name: Compile
        run: python -m compileall -f -q tsao scripts
      - name: Test
        run: python -m pytest -q -p no:cacheprovider tests
      - name: Ruff
        run: python -m ruff check tsao tests scripts
      - name: Build wheel diagnostic
        id: wheel
        shell: bash
        run: |
          set +e
          python -m pip wheel --no-deps --no-build-isolation . -w /tmp/tsao-wheel > /tmp/wheel.txt 2>&1
          status=$?
          cat /tmp/wheel.txt
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Report wheel diagnostic
        if: ${{ steps.wheel.outputs.status != '0' && matrix.python-version == '3.12' && github.event_name == 'pull_request' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo '### Automated Python 3.12 wheel diagnostic'
            echo
            echo '```text'
            cat /tmp/wheel.txt
            echo '```'
          } > /tmp/wheel-comment.md
          gh pr comment "${{ github.event.pull_request.number }}" --body-file /tmp/wheel-comment.md
      - name: Enforce wheel build
        if: ${{ steps.wheel.outputs.status != '0' }}
        run: exit 1
      - name: CLI smoke
        run: |
          python -m tsao.cli route "metallocene EPDM ENB solution polymerization"
          python scripts/run_ci.py
